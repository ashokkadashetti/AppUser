<h1>Hi, <%= current_user.first_name if current_user %></h1>

<table>
  	<thead>
    	<tr>
    		<th>ID</th>
      		<th>NAME</th>
      		<th>EMAIL</th>
      		<th colspan="3"></th>
    	</tr>
  	</thead>
  	<tbody>
  		<% @users.each do |user| %>
  			<tr>
  				<td><%= user.id %></td>
  				<td><%= user.name %></td>
  				<td><%= user.email %></td>
  				<td><%= link_to "Show", user_path(user) %>&nbsp</td>
  				<td><%= link_to "Edit", edit_user_path(user) %>&nbsp</td>
  				<td><%= link_to 'Delete', user_path(user), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  				
  			</tr>
  		<% end -%>
  	</tbody>
</table>
<h2><%= link_to "Create new User", new_user_path%></h2><br><br> 